<!DOCTYPE html>
<html>
  <head>
    <title><%= message %></title>
    <style>
      body {
        font-family: sans-serif;
        color: #333;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
      }
      h1 {
        font-size: 36px;
        margin-bottom: 10px;
      }
      h2 {
        font-size: 24px;
        margin-bottom: 20px;
      }
      pre {
        background-color: #f5f5f5;
        padding: 10px;
        border-radius: 5px;
        white-space: pre-wrap;
        word-wrap: break-word;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1><%= message %></h1>
      <h2><%= error.status %></h2>
      <pre><%= error.stack %></pre>
      <% if (request) { %>
      <h2>Request:</h2>
      <pre><%= JSON.stringify(error.stack, null, 2) %></pre>
      <% } %>
    </div>


    <script src="/stylesheets/test.js"></script>
  </body>
</html>
